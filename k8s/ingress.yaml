apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pilot-app-ingress
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pilot-app
            port:
              number: 80
